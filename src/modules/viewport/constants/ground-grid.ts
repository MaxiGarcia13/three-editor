/** Cell size in metres (matches world-axes major step). */
export const GROUND_CELL_SIZE = 1;

/** Stronger section line every N metres. */
export const GROUND_SECTION_SIZE = 0.5;

/** How far the grid fades out from the camera focus. */
export const GROUND_FADE_DISTANCE = 40;

export const GROUND_CELL_COLOR = '#27272a';
export const GROUND_SECTION_COLOR = '#3f3f46';

/** Soft contact shadow under the model. */
export const GROUND_SHADOW_OPACITY = 0.45;
export const GROUND_SHADOW_BLUR = 2.5;
export const GROUND_SHADOW_SCALE = 20;
export const GROUND_SHADOW_FAR = 8;
export const GROUND_SHADOW_COLOR = '#000000';
