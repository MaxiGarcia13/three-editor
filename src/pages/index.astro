---
import BaseLayout from '@/layouts/base-layout.astro';
import { EditorPreview, EditorSidebar } from '@/modules/editor-shell';
---

<BaseLayout>
  <main class="flex h-screen w-screen overflow-hidden bg-zinc-900 text-white">
    <EditorSidebar client:only="react" />
    <EditorPreview client:only="react" />
  </main>
</BaseLayout>
